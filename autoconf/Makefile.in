
@SET_MAKE@

RM = /bin/rm

DIR_LIST=libtio/src L1_trace_gas/src L1_cloud/src 

all:
	for dir in $(DIR_LIST) ; do \
	  (cd $$dir ; $(MAKE) all); \
	done

install:
	for dir in $(DIR_LIST) ; do \
	  (cd $$dir ; $(MAKE) install); \
	done

clean:
	for dir in $(DIR_LIST) ; do \
	  (cd $$dir ; $(MAKE) clean); \
	done

confclean:
	-$(RM) Makefile Makefile.defines config.status config.log
