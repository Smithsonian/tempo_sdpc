
SRCDIR = L1_trace_gas/src

RM = /bin/rm

@SET_MAKE@

all:
	cd $(SRCDIR); $(MAKE) all

install:
	cd $(SRCDIR); $(MAKE) install

clean:
	cd $(SRCDIR); $(MAKE) clean

confclean:
	-$(RM) Makefile config.status config.log config.cache
