% Incoming directory and filename pattern to watch for:
_P.incoming_dir  = "$PWD/incoming"$;
_P.file_glob     = "*.tar";
_P.order         = "oldest";
_P.wait_sec      = 1;

% Executable to run:
_P.exec_name     = "$PWD/cron_tar.sh"$;

