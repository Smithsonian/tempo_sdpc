TEMPO_SDPC_v4.7.0   2025 Sep
===============================
  * Update destriping algorithms and logic for CLDO4, NO2
  * Update GLER refdata to land_v5, snow_v4
  * libclim:
    - Support GEOS-CF version 2 forecast and analysis products
  * pipeline:
    - Archive INR *.Internal files with filenames unchanged
    - Support using cached solar wavelength calibration in baseline processing
    - Add new 'daily' service to generate destriping correction files
    - Support filtering O3PROF_L2 before generating O3PROF_L3
  * ASDC interface:
    - Rename service 'asdc_s3' to 'asdc_nrt'
    - Modify service 'asdc' to upload to AWS S3 bucket
    - Support uploading ancillary data to AWS S3 bucket
    - Stop uploading RADREF, DSTRHCHO files to ASDC

TEMPO_SDPC_v4.6.1   2025 Jun
===============================
  * Manage NRT uploads in a separate service, asdc_s3
  * Update INR SW to r3.0.4

TEMPO_SDPC_v4.6.0   2025 Jun
===============================
  * O3PROF product update to support initial release
  * INR:
    - configuration update recommended by Carr Astronautics
  * plan:
    - recompute duration of maneuver-interrupted scans to
      ensure consistency with specified number of co-adds.
  * asdc:
    - mark PDR file accepted only when all data products were
      successfully ingested (enables software to handle a common
      situation autonomously).
  * L1_cloud_o4:
    - NRT product now uses option_apply_solshift=0,
      option_apply_radshift=0

TEMPO_SDPC_v4.5.0   2025 Apr
===============================
  * L0_format: Improve interrupt handling to avoid disrupting generation
      of image data granules.
  * Add pipeline infrastructure to generate NRT data products
    - New services: level1b_nrt1, level1b_nrt2, level2_nrt
    - Asdc service updated to support uploading NRT products to AWS S3 bucket
    - Add NRT support to monitoring and reporting scripts
    - Limit number of days NRT products are archived
  * L1_trace_gas: enable NRT product generation
    - Support pre-computing fit-window specific IRR wavelength calibration
  * L1_cloud_o4:
    - NRT product support:
      * Recompute relative azimuth angle
      * Recompute O2O2 product main_data_quality flag
  * plan:
    - Support quantizing the number of scan steps to maximize
      opportunities for validated command block re-use when the
      instrument simulator is not available.
    - Trigger an INR restart before processing evening city lights scans
      (see redmine 248).
  * INR:
    - Change pitch/yaw random walk coefficients based on investigation
      of 2024-10-12 INR anomaly.
    - Support daily automated INR quality check
    - Update default configuration to use GOES-East=G19
  * Support building SDPC software on SAO/ITS network

TEMPO_SDPC_v4.4.3   2024 Oct
===============================
  * Apply image start time correction per redmine #204.
    - Update OTS iocsdpc library from v0.7.1 to v0.8.0
    - L0_format: store iocsdpc content_version attribute in L0 files
    - L0_ccd: call iocsdpc_tweak_image_time_per_redmine_204 as needed
  * L0_ccd: fix issues in handling of storage region dark frames
    - Avoid integer division by zero when granule contains no active
      region darks
    - Use fixed default exposure time for DARK_INT frames.
      Set calibration.exposure_time_per_dark_int_frame to override
  * L1_cloud_o4: avoid missing data when computing GLER interpolation time

TEMPO_SDPC_v4.4.2   2024 Jun
===============================
  * L1_cloud_o4: handle error return from call to gler_albedo (required
    to reprocess data with maximum scan range)
  * L0_ccd: fix uninitialized memory read that occurred in offset correction
    of RADT_L1 file with missing rows.
  * pull_geoscf.sh: Enable downloading from THREDDS server when primary
    GEOS-CF data portal is unavailable
     - replace glob expression with explicit loop over filenames
     - turn off SSL certificate verification
  * bounding polygons: filter non-monotonic longitudes before constructing
    bounding polygon metadata (required for ASDC ingest of RADT_L1 files
    with missing data).
  * Increment product version number assigned to radiance reference files
    (required to avoid radref filename conflicts in V03 reprocessing).

TEMPO_SDPC_v4.4.1   2024 May 24
===============================
  * Fix missing data interpolation failure in stray light correction
  * replace '$LOGNAME' with 'temposdpc' in crontab
  * remove time limit on L0_ccd slurm process
  * change O3PROF parallelization scheme nodes/blocks from 6/10 to 4/16
  * ephemeris trending should skip HK files with no anc_gps group
  * adjust legend placement in ephemeris plot

TEMPO_SDPC_v4.4.0   2024 May 10
===============================
  * Add support for geolocation and ASDC upload of RADT_L1 product.
  * libtio:
    - Avoid generating self-intersecting bounding polygons.
  * L1_inr_prep:
    - Use predicted ephemeris only when no ephemeris telemetry is available.
  * L2_regrid:
    - Preserve discrete states of quality flags in trace gas products
  * Improve sqlite query performance to reduce likelihood database contention.
  * ASDC uploads prioritize newest data products above any reprocessed
    data products.
  * Use string data type for coremetadata attribute to fix HDF5 format
    non-compliance issue.
  * Support enable/disable options for selected pipeline services to
    improve control over pipeline processing flow.
  * Support generating monthly reporting statistics on TEMPO radiance
    measurement opportunities.
  * Support trending GPSR vs DOP ephemerides
  * Cron system improvements:
      - Adjust scheduling and use file locking to avoid conflicts
        between cron tasks.
  * Update OTS software:
      - ioclib: 1.0.0 to 2.1.1
      - iocsdpc: 0.6.0 to 0.7.1
      - INR SW: 2.3.10 to 3.0.2
  * New reference data in $SDPC_REFDATA_DIR:
     trace_gas/data:
       - OMSAO_CU2020_SolarSpec.dat
       - OMSAO_MolecularRing_CU2020.dat
       - OMSAO_O4_223K_Finkenzeller.dat
     o3_profile/data:
       - refdb/toms_347nm_refl_tempo.dat
       - refdb/TEMPO_xtrack_V01_opf_all_xtrack.dat
       - refdb/TEMPO_xtrack_V01_opf_coadded.dat
       - SoftCal/TEMPO_xtrack_v316G07_new.dat
     gler:
       - ???nm/modis_land_v4_d???.nc
       - ???nm/modis_snow_v3_d???.nc
       - ???nm/modis_ocean_v2_d???.nc
     instrument:
       - TEMPO_senscal_batc_deliverable_04102024_v6.0.nc

TEMPO_SDPC_v4.3.0   2024 Feb
============================
  * Add sdpc_version attribute to standard product labeling
  * plan:
     - add mirror_angles tool
     - scan plan file now specifies integration times to microsecond precision
       (to ensure precise values for LONG_INT_LINES, etc).
  * Implement ASDC recommendations to improve data products:
     - increased boundary polygon spatial resolution
     - add time dimension to variables in L3 data products
  * L0_format:
     - Base cache flushing logic on packet timestamps instead of wall-clock time.
       This prevents network delays from affecting packaging of L0 data products.
  * L0_ccd:
     - support median-filtered offset correction
     - change smear correction method from "oclocks" to "timing"
     - introduce l0_ccd_drk.cfg configuration file for DRK and RADT data
     - support obtaining nominal radiance wavelengths from irradiance file
     - allow dark correction to yield negative pixel values
     - optionally include scattering angle in BTDF correction
     - Add solar boresight angles to irradiance files
  * L1_inr_prep:
     - Support using GPSR telemetry points when available.
  * wavecal:
     - Support fitting sparsely sampled spectra for wavelength calibration.
  * L1_trace_gas:
     - fix data quality flag issues
     - make radiance wavelength calibration optional
     - refine AMF calculation
  * Fix GLER interpolation around new year
  * Update L3 grids to increase spatial resolution and geospatial coverage
  * cron:
     - Add utility script to find gaps in GOES data collection
     - Refactor GOES archiving logic to create a stand-alone tool for
       filling gaps in the GOES data collection

TEMPO_SDPC_v4.2.0   2023 Nov
============================
  * Add handler script to recover from INR crashes caused by data dropouts
  * New features for observation planning
    - support starting scans at a user-specified UTC hour
  * Update L0_ccd to more robustly handle data corruption
  * Update to INR release r2.3.10
  * Improve geospatial bounding polygons in product metadata
    - L2 polygons are now closed
    - L3 polygons now bound L2 coverage area
  * Update plan, L0_ccd to match flight boresight geometry from INR
  * Verify sha1 checksums for downloaded GOES imagery
  * Support email alerts upon failure of pipeline service or cron process
  * Refine logic for generating and selecting radiance reference files
  * L1_inr_prep can use the predicted ephemeris if the telemetry stream
    lacks ephemeris data

TEMPO_SDPC_v4.1.0   2023 Oct
============================
Numerous updates based on performance during on-orbit instrument commissioning:
1. Updates to reference data and configuration files:
   * Molecular cross-sections (O2-O2, water vapor)
   * Instrument calibration key data parameters
   * Tuning retrieval algorithm configuration parameters
2. Code changes
   * Level 0-1a CCD processing
     - change octant phase identification method
     - fix flagging neighbors of saturated pixels
     - (ir)radiance uncertainty was overestimated
   * Eliminate time coverage gaps in IRU, SMC telemetry
     delivered to INR SW in L1 radiance granules
   * Wavelength calibration
     - support fitting wavelength-dependent additive
       correction to nominal wavelength grid
     - support generating more diagnostic output
   * O2O2 cloud algorithm
     - change temperature correction coefficients
     - change temperature used for O2O2 SCD correction
     - use dry pressure to compute SCD
     - implement surface topography correction of surface pressure
   * Trace gas algorithm
     - Improve diagnostic output
     - Fix handling of fill values on cloud fraction input
   * NO2 strat/trop separation
     - compute vertical column troposphere uncertainty
     - fix quality flagging issue
   * Revise cloud filter logic for HCHO radiance reference
     calculation
   * Fix problem handling time intervals that span 0h UTC
   * drop unused variables from L1 radiance file
3. Observation planning updates
    * support specifying two-sided margins on maneuver windows
    * support tuning morning/evening scan region width
    * add a comment field to CSV scan plan file
4. Default OTS configuration updated to use INRSW R2.3.8

TEMPO_SDPC_v4.0.0   2023 Mar 28
===============================
   * Make GOES spacecraft references more generic (e.g. GOES-East/West
     instead of GOES-16/17).
   * Support IOC update to package raw GRDDP and json data into tar files.
   * New pipecron service to automate selected maintenance tasks.
   * Add script to simplify pulling data from IOC cache.
   * Add script to feed image input data for reprocessing.
   * plan improvements:
       - mkplan.sh script to simplify tar file creation for delivery to IOC.
       - New visualization scripts plot_mirror_x.py, plot_szamap.py
       - support using a custom CBM to perform blocks of N scans
   * Archive database now tracks ASDC per-file upload disposition
   * Support generating radiance reference file for trace gas retrievals.
   * Support destriping correction of trace gas retrievals.
   * Wavelength calibration reference data format now supports
     cross-track dependence of convolved irradiance reference spectrum.
   * Move pipeline control environment variables into etc/pipe_control.cfg
   * Update GLER database land files to avoid problematic albedo in
     shallow-ocean regions (pixels with land_fraction < 0.5)
   * Change environment variable names:
       SDPC_RUN_DIR_MASTER -> SDPC_PIPE_DIR
              SDPC_RUN_DIR -> SDPC_NODE_DIR
   * Implement ASDC recommendations to improve CF metadata compliance
   * Update L1_cloud_o4 to get number of GEOSCF layers by reading the data files
   * Now recommend using sdpcrun.sh to configure shell environment
   * Added support for autonomous warm restart of INR server.
   * Wavelength calibration supports including model components in irradiance fit.
   * Add scripts to facilitate status monitoring

TEMPO_SDPC_v3.0.0   2022 Mar 01
===============================
   * Observation planning now incorporates spacecraft maneuvers.
   * Support autonomous delivery of maneuver and ephemeris predictions from IOC.
   * L0_format:
        - support handling FPGA test pattern data
   * L0_ccd:
        - new psf-based stray light correction
        - median filter FPA, FPE temperature time series before interpolating
        - DRK_L1 file now contains mean dark, plus 'frames' group with
          the separate dark frames.
   * L1_polcorr: support new polarization correction lookup tables
   * L1_o3_profile: numerous algorithm updates
   * L1_trace_gas:
        - incorporate improvements from GEMS and MEaSUREs experience.
        - support using slit function from irradiance file.
        - support O2-O2 slant column-only product
        - many updates to control files, PCF files, reference data
   * L1_cloud_o4: new O2-O2 cloud product (CLDO4) to replace Raman cloud product (CLDRR)
   * wavecal: support cross-track parallelization to speed IRR wavelength
              calibration with slit function convolution.
   * Support 2D nominal_wavelength array in L1 radiance file
   * Replace NISE product with IMS snow cover product
        - add snow_ice_fraction product variable
        - drop NISE snow/ice cover bits from ground_pixel_quality_flag
   * Support generating new trace gas products: BRO, CHOCHO, H2O
   * New trend service to support trending of science data (DRK, IRR, RAD)
   * Support using GPS ephemeris from Level 0 data stream
   * Restructure post-INR pipeline to create a separate level1b processing stage
   * Add 'asdc' service to automate ASDC product upload.
   * Support uploading ancillary data to ASDC.
   * Improve support for multiple pipeline instances (put etc directory
     in $SDPC_RUN_DIR_MASTER).
   * Improve organization of cron system for downloading ancillary data
   * Support using GEOS-CF composition forecasts and GEOS-CF hourly
     climatology.  (Drop support for NAM grid meteorological forecasts).
   * New libgler library to support GLER lookup tables (geometry-dependent
     Lambertian-equivalent reflectance).
   * New iocpullraw service to pull raw GRDDP telemetry from IOC.
   * Support running O3PROF tasks in a separate cluster partition.
   * Simplify pipeline processing scripts.
   * Support gyro_quality_flag in IRU file.
   * Facilitate reprocessing beginning either from Level 0 or from Level 1.
   * Support converting GPS ephemeris coordinates from J2000 to ECEF
     (Host will deliver J2000 coordinates, but the INR SW requires ECEF).

TEMPO_SDPC_v2.0.0   2020 Nov 10
===============================
   Initial planned release, to be used for the first planned SDPC
   system tests, and requirement verification.
